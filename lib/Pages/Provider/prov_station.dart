import 'package:flutter/material.dart';

class ProvStation extends StatelessWidget {
  const ProvStation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}