import 'package:flutter/material.dart';

class ProvProfile extends StatelessWidget {
  const ProvProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}